# Vite 8 and vite-plugin-pwa report a peer-dependency mismatch that is harmless
# here. This makes every `npm install` / `npm ci` (locally AND on Cloudflare)
# resolve it the lenient way, so builds don't fail on the peer conflict.
legacy-peer-deps=true
